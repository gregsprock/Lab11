@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<h3>Professors</h3>
<hr />
<p>List of professors:</p>
@foreach (var p in Model.Professors)
{
    <p>@Html.DisplayFor(modelItem => p.FirstName)</p>
}

